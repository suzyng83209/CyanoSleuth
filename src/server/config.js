// require('dotenv').config();

module.exports = {
	env: process.env.NODE_ENV,

	// firebase
	firebaseApiKey: process.env.firebaseApiKey,
	firebaseProjectId: process.env.firebaseProjectId,
	firebaseSenderId: process.env.firebaseSenderId
}